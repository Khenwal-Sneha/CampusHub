server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/campushub_college
    username: root
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  main:
    allow-circular-references: true